{
  "name": "codex-usage-mesh-ingress",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --test tests/worker.test.mjs",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm test && npm run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
